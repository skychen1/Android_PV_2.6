版本号：1.0.0
独立成自己的mudole；
支持的厂家和模式：罗丹贝尔（网络），罗丹贝尔（串口），"鸿陆（网络），睿芯联科（本地），芯联（本地单reader），芯联（本地双reader）；

2019/08/20
版本1.0.1
setFilter (String startStr)
1，增加标签过滤功能，参数是标签开始的头标识：（公司代号，8227）
   如果传入的参数是空字符串表示不过滤；


2019/10/18
版本1.0.2
startScan(String deviceId, int timeOut, byte[] ants)
1，重载开始扫描方法：增加参数设置，ants：本次扫描使用的天线集合，为byte类型素组；（目前仅罗丹贝尔的reader支持）
   原方法依然可用，默认使用所有天线扫描


